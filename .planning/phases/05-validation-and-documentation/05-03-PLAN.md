---
phase: 05-validation-and-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/schema.md
  - docs/conversion.md
  - docs/sparql-examples.md
  - tests/unit/test_docs_completeness.py
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "Schema documentation describes all entity types, their properties, and namespaces used in the RDF output"
    - "Conversion documentation explains the three-stage gene mapping algorithm with concrete examples"
    - "SPARQL example queries demonstrate common use cases against the RDF data"
  artifacts:
    - path: "docs/schema.md"
      provides: "RDF schema reference documentation"
      contains: "AdverseOutcomePathway"
    - path: "docs/conversion.md"
      provides: "Conversion process and mapping strategy documentation"
      contains: "false positive"
    - path: "docs/sparql-examples.md"
      provides: "Curated SPARQL query examples"
      contains: "SELECT"
    - path: "tests/unit/test_docs_completeness.py"
      provides: "Tests verifying documentation covers all required topics"
      contains: "test_schema_docs"
  key_links:
    - from: "docs/schema.md"
      to: "src/aopwiki_rdf/rdf/namespaces.py"
      via: "namespace URI references"
      pattern: "aopkb.org/aop_ontology"
    - from: "docs/conversion.md"
      to: "src/aopwiki_rdf/mapping/gene_mapper.py"
      via: "algorithm description"
      pattern: "three-stage|precision filtering"
---

<objective>
Write schema documentation covering RDF structure, namespaces, and entity types; conversion documentation covering the gene mapping algorithm and chemical mapping strategy; and curated SPARQL example queries.

Purpose: Make the RDF output understandable and queryable by downstream consumers who do not read the pipeline source code.
Output: Three markdown files in docs/ directory with property tables, Mermaid diagrams, and SPARQL examples.
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-validation-and-documentation/05-CONTEXT.md
@.planning/phases/05-validation-and-documentation/05-RESEARCH.md

@src/aopwiki_rdf/rdf/namespaces.py
@src/aopwiki_rdf/rdf/writer.py
@src/aopwiki_rdf/mapping/gene_mapper.py
@src/aopwiki_rdf/mapping/chemical_mapper.py
@prefixes.csv
@SPARQLQueries/

<interfaces>
From src/aopwiki_rdf/rdf/writer.py:
- write_aop_rdf: AOPs, Key Events, KERs, Stressors, Chemicals with properties
- write_genes_rdf: Gene associations from HGNC mapping
- write_enriched_rdf: owl:sameAs cross-references to external databases

From src/aopwiki_rdf/mapping/gene_mapper.py:
- Three-stage algorithm: screening (genedict1), precision matching (genedict2), false positive filtering
- False positive filters: single letter aliases, Roman numerals, context analysis
- BridgeDb batch API for identifier resolution

From src/aopwiki_rdf/rdf/namespaces.py:
- NS_* constants for all namespace URIs
- GENES_PREFIXES, ENRICHED_PREFIXES, VOID_PREFIXES constants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema documentation with entity reference and diagrams</name>
  <files>docs/schema.md, docs/sparql-examples.md, tests/unit/test_docs_completeness.py</files>
  <action>
Create `docs/schema.md` with:

1. **Overview section**: Brief description of the AOPWikiRDF dataset, its three output files, and their purposes (pure entities, gene associations, enriched cross-references).

2. **Namespace table**: List all namespace prefixes used in the output files with their full URIs. Source these from `src/aopwiki_rdf/rdf/namespaces.py` and `prefixes.csv`. Organize by category (core AOP, identifiers, ontologies, metadata).

3. **Entity type reference** with per-entity property tables:
   - **aopo:AdverseOutcomePathway**: Properties (dc:identifier, dc:title, dc:description, rdfs:seeAlso, aopo:has_key_event, aopo:has_adverse_outcome, aopo:has_molecular_initiating_event, etc.), their RDF types, cardinality (1 or 0..n), and description
   - **aopo:KeyEvent**: Properties including biological organization level, cell/organ terms
   - **KER** (aopo:KeyEventRelationship): Properties including upstream/downstream KE links
   - **Stressor** (nci:C54571): Properties including chemical associations
   - **Chemical** (cheminf:000000/cheminf:000446): Properties including CAS, identifiers
   - **Gene associations** (edam:data_1025): Properties from AOPWikiRDF-Genes.ttl
   - **Enriched cross-references**: owl:sameAs patterns from AOPWikiRDF-Enriched.ttl

   Read `src/aopwiki_rdf/rdf/writer.py` to discover the exact properties written for each entity type. Do NOT guess -- extract from the writer code.

4. **Mermaid diagram**: Entity relationship diagram showing how AOPs connect to Key Events, KERs, Stressors, Chemicals, and gene associations. Use `graph LR` or `graph TD` layout. Keep it readable (5-8 entity types, key relationships only).

5. **File structure section**: Describe what each output file contains and how they relate (VoID subset declarations).

Create `docs/sparql-examples.md` with 7-10 curated SPARQL queries:
- Find all AOPs (basic listing)
- Get Key Events for a specific AOP
- Find AOPs associated with a specific chemical (by CAS or name)
- Get gene associations for a Key Event
- List all Key Event Relationships
- Find chemicals with cross-references to external databases
- Count entities by type
- Find AOPs by biological organization level

Reference existing queries in `SPARQLQueries/` directory as starting points, but adapt them to work against the current schema (owl:sameAs, HGNC numeric IDs). Include the SPARQL endpoint URL and brief instructions for running queries.

Create `tests/unit/test_docs_completeness.py` with tests:
- test_schema_docs_exist: Assert docs/schema.md exists
- test_schema_covers_all_entity_types: Read docs/schema.md, assert it contains references to all 7 entity types (AdverseOutcomePathway, KeyEvent, KeyEventRelationship, Stressor, Chemical/cheminf, gene/edam, enriched/owl:sameAs)
- test_schema_has_namespace_table: Assert docs/schema.md contains a table with namespace prefixes
- test_schema_has_mermaid_diagram: Assert docs/schema.md contains a mermaid code block
- test_sparql_examples_exist: Assert docs/sparql-examples.md exists
- test_sparql_has_minimum_queries: Count SELECT occurrences in sparql-examples.md, assert >= 7
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_docs_completeness.py -x --timeout=30</automated>
  </verify>
  <done>Schema docs cover all entity types with property tables and Mermaid diagram. SPARQL examples file has 7+ curated queries. All completeness tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Conversion process documentation</name>
  <files>docs/conversion.md, tests/unit/test_docs_completeness.py</files>
  <action>
Create `docs/conversion.md` covering:

1. **Pipeline overview**: High-level flow from AOP-Wiki XML download through parsing, mapping, and RDF generation. Brief description of each stage.

2. **Gene mapping algorithm** -- the three-stage process:
   - **Stage 1: Screening** -- How genedict1 is built from HGNC data (gene symbols + aliases), initial text matching against KE descriptions. Include a concrete example: "For gene symbol TP53 with alias p53, the screening stage searches KE description text for these terms."
   - **Stage 2: Precision matching** -- How genedict2 adds punctuation-delimited variants for context-aware matching. Example: "The symbol 'A' is extended to patterns like '\\bA\\b' with word boundary assertions to avoid matching the letter A in normal text."
   - **Stage 3: False positive filtering** -- The four filter types (single letter aliases, Roman numerals, context analysis, gene-specific rules). Include specific examples from the production filters: GCNT2 alias "II", PPIB alias "B", IV gene. Reference the 14.6% false positive reduction metric.

3. **Chemical mapping strategy**:
   - CAS identifier extraction from AOP-Wiki XML
   - BridgeDb batch API for cross-database identifier resolution
   - Which external databases are mapped (CHEBI, ChemSpider, Wikidata, ChEMBL, PubChem, DrugBank, KEGG, LipidMaps, HMDB)

4. **BridgeDb batch flow**: How batch_xrefs works (chunks of 100, parse_fn + fallback_fn injection, automatic fallback to individual calls on batch failure). Reference the 55x performance improvement.

5. **Output file generation**: Brief description of how the three TTL files are produced from the mapping results.

Read `src/aopwiki_rdf/mapping/gene_mapper.py` and `src/aopwiki_rdf/mapping/chemical_mapper.py` to get accurate algorithm details. Do NOT guess -- describe what the code actually does.

Add test to `tests/unit/test_docs_completeness.py`:
- test_conversion_docs_exist: Assert docs/conversion.md exists
- test_conversion_covers_gene_mapping: Assert docs/conversion.md contains references to all three stages (screening, precision, false positive)
- test_conversion_covers_chemical_mapping: Assert docs/conversion.md mentions BridgeDb and chemical mapping
- test_conversion_has_examples: Assert docs/conversion.md contains at least 3 concrete examples (TP53, GCNT2, or similar gene names that appear in the actual code)
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_docs_completeness.py -x --timeout=30</automated>
  </verify>
  <done>Conversion docs describe three-stage gene mapping with concrete examples, chemical mapping with BridgeDb batch flow, and output file generation. All completeness tests pass.</done>
</task>

</tasks>

<verification>
1. `docs/schema.md` exists with entity reference, namespace table, and Mermaid diagram
2. `docs/conversion.md` exists with gene mapping algorithm walkthrough and chemical mapping
3. `docs/sparql-examples.md` exists with 7+ curated SPARQL queries
4. `python -m pytest tests/unit/test_docs_completeness.py -v` all pass
5. Mermaid diagram renders correctly on GitHub (valid mermaid syntax)
</verification>

<success_criteria>
- Schema docs reference all entity types from all three TTL files
- Property tables show correct property URIs matching the actual RDF output
- Mermaid diagram shows entity relationships clearly
- Conversion docs explain the three-stage algorithm with concrete examples from production code
- SPARQL queries are adapted for current schema (owl:sameAs, numeric HGNC IDs)
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-documentation/05-03-SUMMARY.md`
</output>
